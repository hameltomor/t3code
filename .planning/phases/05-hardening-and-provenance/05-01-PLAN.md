---
phase: 05-hardening-and-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/historyImport.ts
  - packages/contracts/src/ipc.ts
  - apps/server/src/historyImport/fingerprint.ts
  - apps/server/src/historyImport/Services/HistoryImportService.ts
  - apps/server/src/historyImport/Layers/HistoryImportService.ts
  - apps/server/src/historyImport/Layers/CodexHistoryScanner.ts
  - apps/server/src/historyImport/Layers/ClaudeCodeHistoryScanner.ts
  - apps/server/src/wsServer.ts
  - apps/web/src/wsNativeApi.ts
  - apps/web/src/lib/historyImportReactQuery.ts
  - apps/web/src/hooks/useThreadExternalLink.ts
  - apps/web/src/components/ProvenanceCard.tsx
  - apps/web/src/components/ChatView.tsx
autonomous: true

must_haves:
  truths:
    - "Opening an imported thread shows a collapsible provenance bar with provider name, validation status badge, and an expand trigger"
    - "Expanding the provenance bar shows original cwd, imported-at timestamp, link mode, source path, and last validated time"
    - "Opening an imported thread triggers lazy background validation that updates the status badge without blocking the thread view"
    - "Clicking the Validate Link button re-checks source file existence and fingerprint freshness"
    - "The historyImport.validateLink WS method returns the updated validation status instead of an error"
    - "When linkMode is native-resume, the provenance card shows a Continue in Provider button that focuses the chat input"
  artifacts:
    - path: "packages/contracts/src/historyImport.ts"
      provides: "HistoryImportValidateLinkResult schema"
      contains: "HistoryImportValidateLinkResult"
    - path: "apps/server/src/historyImport/fingerprint.ts"
      provides: "Shared computeFingerprint utility"
      exports: ["computeFingerprint"]
    - path: "apps/server/src/historyImport/Services/HistoryImportService.ts"
      provides: "validateLink method on service interface"
      contains: "validateLink"
    - path: "apps/server/src/historyImport/Layers/HistoryImportService.ts"
      provides: "validateLink implementation with file stat + fingerprint check"
      contains: "validateLink"
    - path: "apps/web/src/hooks/useThreadExternalLink.ts"
      provides: "Lazy-fetch hook for thread external link data with auto-validate"
      exports: ["useThreadExternalLink"]
    - path: "apps/web/src/components/ProvenanceCard.tsx"
      provides: "Collapsible provenance card component with Continue in Provider action"
      exports: ["ProvenanceCard"]
  key_links:
    - from: "apps/web/src/components/ChatView.tsx"
      to: "apps/web/src/hooks/useThreadExternalLink.ts"
      via: "useThreadExternalLink hook call in ChatView"
      pattern: "useThreadExternalLink"
    - from: "apps/web/src/hooks/useThreadExternalLink.ts"
      to: "apps/web/src/lib/historyImportReactQuery.ts"
      via: "useQuery with threadLinksQueryOptions + useMutation with validateLinkMutationOptions"
      pattern: "historyImport.*threadLinks|historyImport.*validateLink"
    - from: "apps/web/src/wsNativeApi.ts"
      to: "apps/server/src/wsServer.ts"
      via: "WS transport request for validateLink"
      pattern: "historyImportValidateLink"
    - from: "apps/server/src/historyImport/Layers/HistoryImportService.ts"
      to: "apps/server/src/historyImport/fingerprint.ts"
      via: "import computeFingerprint for validation"
      pattern: "computeFingerprint"
---

<objective>
Implement server-side link validation and thread provenance card UI.

Purpose: FR-9 requires imported threads to display origin metadata and link validation status, including a "Continue in provider" action for native-resume threads. SC-1 and SC-2 require a provenance card in thread view with lazy background validation on thread open. This plan builds the full vertical slice: contracts, shared fingerprint utility, server validateLink method, WS handler, client transport/React Query wiring, and the ProvenanceCard component integrated into ChatView.

Output: Working provenance card that appears on imported threads with lazy background validation updating the status badge asynchronously, and a "Continue in Provider" button for native-resume link mode.
</objective>

<execution_context>
@/home/danil.morozov/.claude/get-shit-done/workflows/execute-plan.md
@/home/danil.morozov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-and-provenance/05-RESEARCH.md
@packages/contracts/src/historyImport.ts
@packages/contracts/src/ipc.ts
@apps/server/src/historyImport/Services/HistoryImportService.ts
@apps/server/src/historyImport/Layers/HistoryImportService.ts
@apps/server/src/historyImport/Layers/CodexHistoryScanner.ts
@apps/server/src/historyImport/Layers/ClaudeCodeHistoryScanner.ts
@apps/server/src/historyImport/Errors.ts
@apps/server/src/wsServer.ts
@apps/server/src/persistence/Services/ThreadExternalLinks.ts
@apps/web/src/wsNativeApi.ts
@apps/web/src/lib/historyImportReactQuery.ts
@apps/web/src/components/ChatView.tsx
@apps/web/src/types.ts
@apps/web/src/nativeApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared fingerprint utility, scanner refactoring, and contracts</name>
  <files>
    apps/server/src/historyImport/fingerprint.ts
    apps/server/src/historyImport/Layers/CodexHistoryScanner.ts
    apps/server/src/historyImport/Layers/ClaudeCodeHistoryScanner.ts
    packages/contracts/src/historyImport.ts
    packages/contracts/src/ipc.ts
  </files>
  <action>
    1. **Create shared fingerprint utility** at `apps/server/src/historyImport/fingerprint.ts`:
       - Extract the `computeFingerprint(sessionId: string, filePath: string)` function from `CodexHistoryScanner.ts` (lines 47-85). It uses `createHash("sha256")` from `node:crypto`, `open` and `stat` from `node:fs/promises`, a `SAMPLE_SIZE = 4096` constant, and returns `Effect.Effect<string, HistoryImportScanError>`.
       - The algorithm: stat file for size/mtime, read head buffer (min(SAMPLE_SIZE, fileSize)), read tail buffer if fileSize > SAMPLE_SIZE, hash sessionId + fileSize + mtimeMs + headBuf + tailBuf.
       - Export the function and the `SAMPLE_SIZE` constant.

    2. **Refactor both scanners** to import from the shared utility:
       - In `CodexHistoryScanner.ts`: Remove the local `computeFingerprint` function and `SAMPLE_SIZE` constant (lines 45-85). Add `import { computeFingerprint } from "../fingerprint.ts"`. All call sites remain unchanged.
       - In `ClaudeCodeHistoryScanner.ts`: Remove the local `computeFingerprint` function and `SAMPLE_SIZE` constant (lines 33-97). Add `import { computeFingerprint } from "../fingerprint.ts"`. All call sites remain unchanged.
       - Also remove the `import { createHash } from "node:crypto"` and `import { open, stat } from "node:fs/promises"` from both scanners IF they are only used by the removed computeFingerprint. Check carefully -- `stat` and `open` may be used elsewhere in the scanner (e.g., `fileExists` function in CodexHistoryScanner uses `stat`). Only remove imports that are no longer referenced.

    3. **Add HistoryImportValidateLinkResult schema** to `packages/contracts/src/historyImport.ts`:
       ```typescript
       export const HistoryImportValidateLinkResult = Schema.Struct({
         threadId: ThreadId,
         validationStatus: HistoryImportValidationStatus,
         lastValidatedAt: IsoDateTime,
       });
       export type HistoryImportValidateLinkResult = typeof HistoryImportValidateLinkResult.Type;
       ```

    4. **Add validateLink to NativeApi interface** in `packages/contracts/src/ipc.ts`:
       - Add import for `HistoryImportValidateLinkInput` and `HistoryImportValidateLinkResult` from `./historyImport`.
       - Add to the `historyImport` section of `NativeApi`:
         `validateLink: (input: HistoryImportValidateLinkInput) => Promise<HistoryImportValidateLinkResult>;`

    **Important patterns to follow:**
    - Use `Effect.catch` (not `Effect.catchAll`) -- per Phase 4 decisions.
    - Avoid try/catch inside Effect generators -- use Effect combinators.
  </action>
  <verify>
    Run `bun typecheck` -- must pass with 0 errors. Run `bun lint` -- must pass. Verify the shared fingerprint import works by checking both scanners still reference `computeFingerprint` correctly.
  </verify>
  <done>
    - Shared `computeFingerprint` in `fingerprint.ts`, imported by both scanners
    - `HistoryImportValidateLinkResult` schema exists in contracts
    - `NativeApi.historyImport.validateLink` exists in ipc.ts
    - Both scanners compile cleanly with the shared import
  </done>
</task>

<task type="auto">
  <name>Task 2: validateLink server implementation, WS handler, and client wiring</name>
  <files>
    apps/server/src/historyImport/Services/HistoryImportService.ts
    apps/server/src/historyImport/Layers/HistoryImportService.ts
    apps/server/src/wsServer.ts
    apps/web/src/wsNativeApi.ts
    apps/web/src/lib/historyImportReactQuery.ts
  </files>
  <action>
    1. **Add validateLink to HistoryImportServiceShape** in `apps/server/src/historyImport/Services/HistoryImportService.ts`:
       - Import `HistoryImportValidateLinkInput` and `HistoryImportValidateLinkResult` from `@xbetools/contracts`.
       - Add to the interface:
         ```typescript
         readonly validateLink: (
           input: HistoryImportValidateLinkInput,
         ) => Effect.Effect<HistoryImportValidateLinkResult, HistoryImportError>;
         ```

    2. **Implement validateLink** in `apps/server/src/historyImport/Layers/HistoryImportService.ts`:
       - Import `ThreadExternalLinkRepository` from `../../persistence/Services/ThreadExternalLinks.ts`.
       - Import `computeFingerprint` from `../fingerprint.ts`.
       - Import `stat` from `node:fs/promises`.
       - Import `Option` from `effect`.
       - Yield `ThreadExternalLinkRepository` in `makeHistoryImportService` Effect.gen alongside the existing services.
       - Implement `validateLink` method:
         a. Look up external link via `externalLinkRepo.getByThreadId({ threadId: input.threadId as string })`. Map errors to `HistoryImportNotFoundError`.
         b. If `Option.isNone(maybeLink)`, yield `HistoryImportNotFoundError`.
         c. Check file existence: `Effect.tryPromise({ try: () => stat(link.sourcePath).then(() => true), catch: () => false })`.
         d. If file missing: upsert with `validationStatus: "missing"`, return `{ threadId: input.threadId, validationStatus: "missing", lastValidatedAt: now }`.
         e. Recompute fingerprint via `computeFingerprint(link.providerSessionId ?? (link.threadId as string), link.sourcePath)`. Wrap in `Effect.catch(() => Effect.succeed(null as string | null))` to handle errors gracefully.
         f. Determine status: `null` fingerprint -> "invalid", matches stored -> "valid", differs -> "stale".
         g. Upsert updated link with new validationStatus and lastValidatedAt.
         h. Return `{ threadId: input.threadId, validationStatus, lastValidatedAt: now }` cast to `HistoryImportValidateLinkResult`.
         i. Wrap with `Effect.withSpan("HistoryImportService.validateLink")`.
       - Add `validateLink` to the returned `satisfies HistoryImportServiceShape` object.

    3. **Replace validateLink stub** in `apps/server/src/wsServer.ts`:
       - Find the `case WS_METHODS.historyImportValidateLink` block (around line 1117-1121).
       - Replace the `RouteRequestError` stub with:
         ```typescript
         case WS_METHODS.historyImportValidateLink: {
           const body = stripRequestTag(request.body);
           const result = yield* historyImportService.validateLink(body).pipe(
             Effect.mapError(
               (error) => new RouteRequestError({ message: error.message }),
             ),
           );
           return result;
         }
         ```

    4. **Add validateLink to wsNativeApi** in `apps/web/src/wsNativeApi.ts`:
       - In the `historyImport` object, add:
         `validateLink: (input) => transport.request(WS_METHODS.historyImportValidateLink, input),`
       - Note: `WS_METHODS.historyImportValidateLink` is already registered in the WS methods constant (it maps to `HISTORY_IMPORT_WS_METHODS.validateLink` which is registered in `ws.ts`).

    5. **Add React Query options** to `apps/web/src/lib/historyImportReactQuery.ts`:
       - Add to `historyImportQueryKeys`: `threadLinks: (threadId: string) => ["historyImport", "threadLinks", threadId] as const,`
       - Add `historyImportThreadLinksQueryOptions(threadId: string | null)`:
         ```typescript
         export function historyImportThreadLinksQueryOptions(threadId: string | null) {
           return queryOptions({
             queryKey: historyImportQueryKeys.threadLinks(threadId ?? ""),
             queryFn: async () => {
               const api = ensureNativeApi();
               return api.historyImport.listThreadLinks({ threadId: threadId! });
             },
             enabled: threadId !== null,
             staleTime: 60_000,
           });
         }
         ```
       - Add `historyImportValidateLinkMutationOptions`:
         ```typescript
         export function historyImportValidateLinkMutationOptions(input: { queryClient: QueryClient }) {
           return mutationOptions({
             mutationKey: ["historyImport", "mutation", "validateLink"] as const,
             mutationFn: async (params: { threadId: string }) => {
               const api = ensureNativeApi();
               return api.historyImport.validateLink({ threadId: params.threadId });
             },
             onSuccess: async (_data, variables) => {
               await input.queryClient.invalidateQueries({
                 queryKey: historyImportQueryKeys.threadLinks(variables.threadId),
               });
             },
           });
         }
         ```
       - Import `HistoryImportValidateLinkInput` type if needed for the mutation params. Use the simpler `{ threadId: string }` shape since that's what the input schema requires after stripping branded types.

    **Important patterns to follow:**
    - Use `Effect.catch` (not `Effect.catchAll`) -- per Phase 4 decisions.
    - Avoid try/catch inside Effect generators -- use Effect combinators.
    - Use `Effect.tryPromise` for async operations.
    - Cast IsoDateTime and branded types at boundaries following existing patterns (e.g., `now as typeof IsoDateTime.Type`).
  </action>
  <verify>
    Run `bun typecheck` -- must pass with 0 errors. Run `bun lint` -- must pass. Verify the WS handler no longer returns the stub error.
  </verify>
  <done>
    - `validateLink` method exists on `HistoryImportServiceShape` and `HistoryImportServiceLive`
    - WS handler for `historyImport.validateLink` returns real validation result (not stub error)
    - `wsNativeApi.historyImport.validateLink` calls through WS transport
    - React Query options for `threadLinks` query and `validateLink` mutation exist
  </done>
</task>

<task type="auto">
  <name>Task 3: ProvenanceCard component, useThreadExternalLink hook, and ChatView integration</name>
  <files>
    apps/web/src/hooks/useThreadExternalLink.ts
    apps/web/src/components/ProvenanceCard.tsx
    apps/web/src/components/ChatView.tsx
  </files>
  <action>
    1. **Create useThreadExternalLink hook** at `apps/web/src/hooks/useThreadExternalLink.ts`:
       - Import `useQuery`, `useMutation`, `useQueryClient` from `@tanstack/react-query`.
       - Import `useEffect` from `react`.
       - Import `historyImportThreadLinksQueryOptions`, `historyImportValidateLinkMutationOptions` from `~/lib/historyImportReactQuery`.
       - Import `type ThreadExternalLink` from `@xbetools/contracts`.
       - The hook signature: `useThreadExternalLink(threadId: string, isImported: boolean)`.
       - Create a `linkQuery` using `useQuery(historyImportThreadLinksQueryOptions(isImported ? threadId : null))`.
       - Derive `externalLink` as `linkQuery.data?.[0] ?? null` (single link per thread).
       - Create `validateMutation` using `useMutation(historyImportValidateLinkMutationOptions({ queryClient }))`.
       - Add a `useEffect` that triggers validation when:
         a. `externalLink` is non-null AND
         b. `shouldRevalidate(externalLink)` returns true (check if `lastValidatedAt` is null OR older than 1 hour: `!externalLink.lastValidatedAt || Date.now() - new Date(externalLink.lastValidatedAt).getTime() > 3_600_000`) AND
         c. `validateMutation.isPending` is false (avoid re-trigger while validating).
         If all conditions met, call `validateMutation.mutate({ threadId })`.
       - Return `{ externalLink, isLoading: linkQuery.isLoading, isValidating: validateMutation.isPending, validate: () => validateMutation.mutate({ threadId }) }`.

    2. **Create ProvenanceCard component** at `apps/web/src/components/ProvenanceCard.tsx`:
       - Import UI primitives: `Badge` from `./ui/badge`, `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent` from `./ui/collapsible`, `Button` from `./ui/button`.
       - Import icons: `LinkIcon`, `ChevronDownIcon`, `PlayIcon` from `lucide-react`.
       - Import `useState` from `react`.
       - Import `type ThreadExternalLink` from `@xbetools/contracts`.
       - Props: `{ externalLink: ThreadExternalLink; onValidate: () => void; isValidating: boolean; onContinueInProvider?: () => void }`.
       - Map `validationStatus` to badge variants:
         - `"valid"` -> variant `"success"`, label `"Valid"`
         - `"missing"` -> variant `"error"`, label `"Source Missing"`
         - `"stale"` -> variant `"warning"`, label `"Source Changed"`
         - `"invalid"` -> variant `"error"`, label `"Invalid"`
         - `"unknown"` -> variant `"outline"`, label `"Not Validated"`
         - `"importing"` -> variant `"warning"`, label `"Partial Import"` (future-proof for Plan 02)
         - Default fallback to `"outline"` variant.
       - Map `providerName` to display label: `"codex"` -> `"Codex"`, `"claudeCode"` -> `"Claude Code"`, default to the raw value.
       - Render as a collapsible:
         - Trigger line: compact bar with `LinkIcon` (size-3), "Imported from {providerLabel}", validation `Badge`, and a `ChevronDownIcon` that rotates when open.
         - Collapsed content: a div with text-xs metadata rows:
           - Provider: {providerLabel}
           - Original CWD: {externalLink.originalCwd}
           - Imported: {formatted importedAt using `Intl.DateTimeFormat`}
           - Link Mode: {externalLink.linkMode}
           - Source: {externalLink.sourcePath}
           - Last Validated: {formatted lastValidatedAt or "Never"}
         - Footer with action buttons:
           - "Validate Link" `Button` (size="sm", variant="outline") that calls `onValidate`, disabled when `isValidating`.
           - **Conditionally** when `externalLink.linkMode === "native-resume"` AND `onContinueInProvider` is provided: render a "Continue in Provider" `Button` (size="sm", variant="default") with a `PlayIcon` (size-3) that calls `onContinueInProvider`. This satisfies FR-9 AC "Continue in provider" action. The button label should include the provider name: "Continue in {providerLabel}".
       - Styling: use `border-b` on the trigger line to separate it from the chat messages below. Use muted-foreground text colors and text-xs for the detail rows. The collapsible content area should have `px-4 py-2` padding.
       - Default state: collapsed (`useState(false)`).

    3. **Integrate into ChatView**:
       - In `ChatView.tsx`, find the `ChatView` function component (export default around line 614).
       - Import `useThreadExternalLink` from `~/hooks/useThreadExternalLink`.
       - Import `ProvenanceCard` from `./ProvenanceCard`.
       - Import `useCallback` from `react` (if not already imported).
       - After the existing `const serverThread = useThread(threadId);` line, add:
         ```typescript
         const isImported = serverThread?.providerThreadId !== null;
         const { externalLink, isValidating, validate } = useThreadExternalLink(threadId, isImported);
         ```
       - Create a `handleContinueInProvider` callback that focuses the chat input area. Look for the existing chat input ref (likely a ref to a textarea or the ChatInput component). The simplest implementation: find the textarea in the chat input section and call `.focus()` on it. If there is a ref like `inputRef` or `textareaRef`, use that. If not, use `document.querySelector` as fallback:
         ```typescript
         const handleContinueInProvider = useCallback(() => {
           // Focus the chat input so user can type their next message
           // The native resume flow is automatic -- ProviderCommandReactor detects
           // the ThreadExternalLink and constructs the resume cursor when a message is sent
           const textarea = document.querySelector<HTMLTextAreaElement>('[data-chat-input]');
           textarea?.focus();
         }, []);
         ```
       - Find where the `ChatHeader` component is rendered (around line 3620). Immediately AFTER the closing tag of the header `</div>` (the div containing ChatHeader), render the provenance card conditionally:
         ```tsx
         {externalLink && (
           <ProvenanceCard
             externalLink={externalLink}
             onValidate={validate}
             isValidating={isValidating}
             onContinueInProvider={
               externalLink.linkMode === "native-resume" ? handleContinueInProvider : undefined
             }
           />
         )}
         ```
       - This places the provenance bar between the header and the message list, visible only for imported threads with an external link record.
       - IMPORTANT: The provenance card renders OUTSIDE the header's drag-region div, in its own non-draggable container. Do NOT put it inside the `drag-region` header div.

    **Important patterns:**
    - Follow existing Badge component API: `<Badge variant="success">Valid</Badge>` (check `apps/web/src/components/ui/badge.tsx` for exact variant names).
    - Follow existing Collapsible usage pattern from Sidebar.tsx imports.
    - Use `type` keyword for type-only imports: `import type { ThreadExternalLink } from "@xbetools/contracts"`.
    - Content-based React list keys (per Phase 3 decisions), but this component doesn't render lists.
    - The hook should NOT block thread view rendering -- the `linkQuery` runs in the background, and the ProvenanceCard only appears when data arrives.
    - The "Continue in Provider" button leverages the existing Phase 4 native resume infrastructure -- when the user sends any message on an imported thread with native-resume linkMode, ProviderCommandReactor automatically constructs the resume cursor from the ThreadExternalLink data. The button's job is simply to direct the user to the chat input.
  </action>
  <verify>
    Run `bun typecheck` -- must pass with 0 errors. Run `bun lint` -- must pass. Verify ProvenanceCard renders correctly by checking that the component imports exist and the ChatView integration point is syntactically valid.
  </verify>
  <done>
    - `useThreadExternalLink` hook returns external link data, validation state, and validate action
    - `ProvenanceCard` component renders collapsible provenance bar with validation badge and metadata
    - ProvenanceCard shows "Continue in {Provider}" button when linkMode is native-resume (FR-9 AC)
    - ChatView conditionally renders ProvenanceCard below header for imported threads
    - Lazy validation triggers automatically on thread open when last validation is stale (>1 hour)
    - Manual "Validate Link" button triggers on-demand validation
  </done>
</task>

</tasks>

<verification>
1. `bun typecheck` passes with 0 errors
2. `bun lint` passes
3. The `historyImport.validateLink` WS method stub is replaced with real implementation
4. Both CodexHistoryScanner and ClaudeCodeHistoryScanner import from shared `fingerprint.ts`
5. `NativeApi.historyImport.validateLink` exists in ipc.ts
6. ProvenanceCard component exists and is wired into ChatView
7. ProvenanceCard shows "Continue in {Provider}" button for native-resume threads
</verification>

<success_criteria>
- Opening an imported thread fetches external link data via `listThreadLinks` query and renders a provenance bar below the header
- The provenance bar shows provider name and a validation status badge in a compact trigger line
- Expanding the provenance bar shows full metadata (originalCwd, importedAt, linkMode, sourcePath, lastValidatedAt)
- Background validation triggers automatically on thread open when last validation is older than 1 hour
- The "Validate Link" button triggers on-demand validation and the badge updates when complete
- For native-resume threads, a "Continue in {Provider}" button appears that focuses the chat input
- Native (non-imported) threads show no provenance bar
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-and-provenance/05-01-SUMMARY.md`
</output>
