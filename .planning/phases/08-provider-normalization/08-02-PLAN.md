---
phase: 08-provider-normalization
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/server/src/provider/Layers/ClaudeCodeAdapter.ts
  - apps/server/src/provider/Layers/GeminiAdapter.ts
  - apps/server/src/provider/normalization/tokenUsageNormalization.test.ts
autonomous: true

must_haves:
  truths:
    - "Claude Code adapter emits thread.token-usage.updated with NormalizedTokenUsage on result messages and emits compaction-correlated token usage on compact_boundary"
    - "Gemini adapter emits thread.token-usage.updated with NormalizedTokenUsage from usageMetadata after each generateContent response, and countTokens failures never block turns"
    - "Cross-provider fixture tests verify normalization of real Codex, Claude, and Gemini raw usage payloads into canonical NormalizedTokenUsage"
  artifacts:
    - path: "apps/server/src/provider/Layers/ClaudeCodeAdapter.ts"
      provides: "Claude Code token usage emission with support=derived-live and compaction correlation"
      contains: "normalizeClaudeUsage"
    - path: "apps/server/src/provider/Layers/GeminiAdapter.ts"
      provides: "Gemini token usage emission with support=derived-on-demand from usageMetadata"
      contains: "normalizeGeminiUsage"
    - path: "apps/server/src/provider/normalization/tokenUsageNormalization.test.ts"
      provides: "Cross-provider normalization fixture tests"
      contains: "normalizeCodexUsage"
  key_links:
    - from: "apps/server/src/provider/Layers/ClaudeCodeAdapter.ts"
      to: "apps/server/src/provider/normalization/tokenUsageNormalization.ts"
      via: "import normalizeClaudeUsage"
      pattern: "normalizeClaudeUsage"
    - from: "apps/server/src/provider/Layers/GeminiAdapter.ts"
      to: "apps/server/src/provider/normalization/tokenUsageNormalization.ts"
      via: "import normalizeGeminiUsage"
      pattern: "normalizeGeminiUsage"
    - from: "apps/server/src/provider/normalization/tokenUsageNormalization.test.ts"
      to: "apps/server/src/provider/normalization/tokenUsageNormalization.ts"
      via: "import all three normalization functions"
      pattern: "normalize.*Usage"
---

<objective>
Wire Claude Code and Gemini adapters to emit typed `thread.token-usage.updated` events using the normalization module from 08-01, and add cross-provider fixture tests verifying all three normalizers.

Purpose: Completes provider normalization so all three adapters emit uniform token usage events with support tier labeling. Fixture tests lock down the normalization contracts with real-world payload shapes.

Output: Claude Code adapter emits on result + compact_boundary, Gemini adapter emits from usageMetadata, and fixture tests cover all three providers.
</objective>

<execution_context>
@/home/danil.morozov/.claude/get-shit-done/workflows/execute-plan.md
@/home/danil.morozov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-provider-normalization/08-RESEARCH.md
@.planning/phases/08-provider-normalization/08-01-SUMMARY.md
@apps/server/src/provider/Layers/ClaudeCodeAdapter.ts
@apps/server/src/provider/Layers/GeminiAdapter.ts
@apps/server/src/provider/normalization/tokenUsageNormalization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Claude Code and Gemini adapter token usage emission</name>
  <files>
    apps/server/src/provider/Layers/ClaudeCodeAdapter.ts
    apps/server/src/provider/Layers/GeminiAdapter.ts
  </files>
  <action>
**Claude Code adapter** (`ClaudeCodeAdapter.ts`):

1. Import `normalizeClaudeUsage` from `../normalization/tokenUsageNormalization.ts`.

2. In `handleResultMessage` (line ~1205-1222), after the `completeTurn` call (line 1221), add token usage emission. The `message` parameter is typed `SDKMessage` but at this point we know `message.type === "result"`. Access `message.usage` (type `NonNullableUsage` with snake_case fields: `input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`). If `message.usage` exists:
   - Call `normalizeClaudeUsage(message.usage)` to get `NormalizedTokenUsage`
   - Emit `offerRuntimeEvent` with type `"thread.token-usage.updated"`, the standard event stamp and base fields (`eventId`, `provider: PROVIDER`, `createdAt`, `threadId: context.sessionKey`), `providerRefs` from `providerThreadRef(context)`, and payload: `{ usage: normalizedUsage, support: "derived-live" as const, source: "sdk-usage" as const }`
   - The `message.usage` field is typed as `any` on SDKMessage since the SDK types use a discriminated union. Use a defensive check: `if (message.usage && typeof message.usage === "object" && "input_tokens" in message.usage)`.

3. In `handleSystemMessage` (line ~1224), in the `compact_boundary` case (line 1273-1282), AFTER the existing `thread.state.changed` event emission, add a SECOND event emission for `thread.token-usage.updated`. Access `message.compact_metadata?.pre_tokens` if available. Emit the token usage event with:
   - `usage: { totalTokens: message.compact_metadata?.pre_tokens ?? 0 }` (minimal NormalizedTokenUsage with just the pre-compaction total)
   - `support: "derived-live" as const`
   - `source: "sdk-usage" as const`
   - This correlates the compaction with the last known token count. Phase 9 ProviderRuntimeIngestion will use this to set compaction metadata on the context status.
   - Use a separate `makeEventStamp()` call for the second event (each event needs its own unique ID).

**Gemini adapter** (`GeminiAdapter.ts`):

1. Import `normalizeGeminiUsage` from `../normalization/tokenUsageNormalization.ts`.

2. In the `completeTurn` helper (line ~218-280), BEFORE the `ctx.turnState = undefined` line (line 260), add token usage emission. Access the last `GenerateContentResponse` from the turn. The problem is that `response` is not available in `completeTurn` -- it's available in the tool loop caller. Two approaches:
   - **Preferred**: Store the last `usageMetadata` on the turn state. Add a `lastUsageMetadata` field to the turn state type. After each `callGemini` response (line ~547), save `response.usageMetadata` onto the turn state: `ts.lastUsageMetadata = response.usageMetadata`. Then in `completeTurn`, read it from `ctx.turnState.lastUsageMetadata`.
   - Emit `thread.token-usage.updated` with `{ usage: normalizeGeminiUsage(ts.lastUsageMetadata), support: "derived-on-demand" as const, source: "sdk-usage" as const }`.
   - Only emit if `ts.lastUsageMetadata` exists (it won't if the turn was aborted before any Gemini call completed).

3. Ensure `countTokens` is NOT called anywhere in the adapter. Per PROV-05, we rely solely on `usageMetadata` from `generateContent` responses. If `countTokens` calls exist, they should be wrapped in try/catch that swallows failures. As of current code, `countTokens` is not called, so this is a no-op verification.
  </action>
  <verify>
Run `bun typecheck` -- full workspace must compile. Run `bun lint` -- must pass. Run `bun run test` in `apps/server` -- all existing tests must pass. Visually confirm Claude Code adapter emits `thread.token-usage.updated` in both `handleResultMessage` and `compact_boundary` handler. Visually confirm Gemini adapter emits `thread.token-usage.updated` in `completeTurn`.
  </verify>
  <done>
Claude Code adapter emits `thread.token-usage.updated` on result messages (support="derived-live", source="sdk-usage") and on compact_boundary (with pre_tokens). Gemini adapter emits `thread.token-usage.updated` from usageMetadata (support="derived-on-demand", source="sdk-usage"). No countTokens calls block turns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-provider normalization fixture tests</name>
  <files>
    apps/server/src/provider/normalization/tokenUsageNormalization.test.ts
  </files>
  <action>
Create `apps/server/src/provider/normalization/tokenUsageNormalization.test.ts` with fixture-based tests for all three normalization functions.

Use `describe`/`it` from `vitest`. Import `normalizeCodexUsage`, `normalizeClaudeUsage`, `normalizeGeminiUsage` and their raw type interfaces from `./tokenUsageNormalization.ts`.

**Test suites:**

1. `describe("normalizeCodexUsage")`:
   - "normalizes a real Codex token usage payload" -- use realistic fixture: `{ total: { inputTokens: 15420, outputTokens: 3200, cachedInputTokens: 8000, reasoningOutputTokens: 1200, totalTokens: 18620 }, last: { inputTokens: 5420, outputTokens: 1200, cachedInputTokens: 3000, reasoningOutputTokens: 400, totalTokens: 6620 }, modelContextWindow: 400000 }`. Assert result equals `{ inputTokens: 15420, outputTokens: 3200, totalTokens: 18620, cachedInputTokens: 8000, reasoningTokens: 1200 }`.
   - "handles zero values" -- all zeros in total. Assert all result fields are 0.
   - "ignores last breakdown and modelContextWindow" -- verify only `total` is used.

2. `describe("normalizeClaudeUsage")`:
   - "normalizes a real Claude usage payload" -- use realistic fixture: `{ input_tokens: 12000, output_tokens: 4500, cache_creation_input_tokens: 2000, cache_read_input_tokens: 6000 }`. Assert result equals `{ inputTokens: 12000, outputTokens: 4500, totalTokens: 16500, cachedInputTokens: 6000 }`.
   - "does not map cache_creation_input_tokens" -- verify it's excluded from result (already counted in input_tokens).

3. `describe("normalizeGeminiUsage")`:
   - "normalizes a real Gemini usage payload" -- use realistic fixture: `{ promptTokenCount: 8000, candidatesTokenCount: 2000, totalTokenCount: 10000, cachedContentTokenCount: 3000, thoughtsTokenCount: 500 }`. Assert result equals `{ inputTokens: 8000, outputTokens: 2000, totalTokens: 10000, cachedInputTokens: 3000, reasoningTokens: 500 }`.
   - "handles all-undefined fields" -- pass `{}`. Assert result equals `{ inputTokens: 0, outputTokens: 0, totalTokens: 0 }` (no optional fields present).
   - "computes totalTokens when totalTokenCount is undefined" -- pass `{ promptTokenCount: 5000, candidatesTokenCount: 1000 }`. Assert `totalTokens` is 6000.
   - "omits optional fields when source fields are undefined" -- pass `{ promptTokenCount: 5000, candidatesTokenCount: 1000, totalTokenCount: 6000 }` (no cached/reasoning). Assert result has no `cachedInputTokens` or `reasoningTokens` keys.
  </action>
  <verify>
Run `bun run test` in `apps/server` -- all tests including the new normalization tests must pass. Verify test count increased.
  </verify>
  <done>
Cross-provider fixture tests verify normalization of Codex (3 cases), Claude (2 cases), and Gemini (4 cases) raw usage payloads into canonical NormalizedTokenUsage. All 9 test cases pass.
  </done>
</task>

</tasks>

<verification>
1. `bun typecheck` passes for the full workspace (7 packages)
2. `bun lint` passes with 0 errors
3. `bun run test` in `apps/server` passes all tests (existing + 9 new normalization tests)
4. Claude Code adapter emits `thread.token-usage.updated` in `handleResultMessage` with `support: "derived-live"` and in `compact_boundary` handler
5. Gemini adapter emits `thread.token-usage.updated` from `usageMetadata` with `support: "derived-on-demand"`
6. All three adapter support tiers are correctly labeled: Codex="native", Claude="derived-live", Gemini="derived-on-demand"
</verification>

<success_criteria>
- Claude Code adapter emits token usage events on result messages and compact_boundary
- Gemini adapter emits token usage events from generateContent response usageMetadata
- countTokens failures never block turns (no countTokens calls in adapter)
- 9 fixture tests pass covering all three normalization functions
- Full typecheck, lint, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-provider-normalization/08-02-SUMMARY.md`
</output>
