---
phase: 03-import-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/components/ImportWizard/steps/PreviewStep.tsx
  - apps/web/src/components/ImportWizard/steps/ImportOptionsStep.tsx
  - apps/web/src/components/ImportWizard/steps/ResultStep.tsx
  - apps/web/src/components/ImportWizard/ImportWizard.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking a session in step 2 advances the wizard to step 3 (preview) once preview data loads"
    - "Step 3 shows a transcript preview with message samples and activity samples, and displays warnings from the server"
    - "Step 4 shows import configuration options (title, model, runtime mode, interaction mode, link mode) with defaults from the session summary"
    - "Clicking Import in step 4 executes the import and shows a progress spinner"
    - "Step 5 shows the import result with message/activity counts, link mode, and a clickable link to navigate to the created thread"
    - "A toast notification confirms the import with counts after successful execution"
    - "Import errors display in the wizard without crashing the dialog"
  artifacts:
    - path: "apps/web/src/components/ImportWizard/steps/PreviewStep.tsx"
      provides: "Step 3: Transcript preview with warnings"
    - path: "apps/web/src/components/ImportWizard/steps/ImportOptionsStep.tsx"
      provides: "Step 4: Import configuration form"
    - path: "apps/web/src/components/ImportWizard/steps/ResultStep.tsx"
      provides: "Step 5: Result display with thread navigation"
  key_links:
    - from: "apps/web/src/components/ImportWizard/ImportWizard.tsx"
      to: "apps/web/src/lib/historyImportReactQuery.ts"
      via: "useQuery for preview, useMutation for execute"
      pattern: "historyImportPreviewQueryOptions|historyImportExecuteMutationOptions"
    - from: "apps/web/src/components/ImportWizard/steps/ResultStep.tsx"
      to: "@tanstack/react-router"
      via: "useNavigate to thread"
      pattern: "navigate.*threadId"
    - from: "apps/web/src/components/ImportWizard/ImportWizard.tsx"
      to: "apps/web/src/components/ui/toast.tsx"
      via: "toastManager.add on successful import"
      pattern: "toastManager\\.add"
---

<objective>
Complete the import wizard by implementing steps 3 (transcript preview with warnings), 4 (import options form), and 5 (result with thread navigation), wiring the preview query and execute mutation, and adding toast notification on successful import.

Purpose: Delivers the full 5-step import wizard end-to-end, allowing users to preview transcripts, configure options, execute imports, and navigate to the created thread.

Output: Complete, functional import wizard with all 5 steps operational.
</objective>

<execution_context>
@/home/danil.morozov/.claude/get-shit-done/workflows/execute-plan.md
@/home/danil.morozov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-import-ui/03-RESEARCH.md
@.planning/phases/03-import-ui/03-01-SUMMARY.md

# Key source references
@packages/contracts/src/historyImport.ts
@apps/web/src/lib/historyImportReactQuery.ts
@apps/web/src/components/ImportWizard/ImportWizard.tsx
@apps/web/src/components/ImportWizard/useImportWizardReducer.ts
@apps/web/src/components/ImportWizard/steps/ProviderSelectStep.tsx
@apps/web/src/components/ImportWizard/steps/SessionListStep.tsx
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/select.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/toast.tsx
@apps/web/src/store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PreviewStep and ImportOptionsStep components</name>
  <files>
    apps/web/src/components/ImportWizard/steps/PreviewStep.tsx
    apps/web/src/components/ImportWizard/steps/ImportOptionsStep.tsx
  </files>
  <action>
**1. Create `PreviewStep.tsx` (Step 3):**

Props: `{ preview: HistoryImportConversationPreview | null; isLoading: boolean; error: string | null }`

Render:
- **Loading state**: When `isLoading`, show a centered spinner with "Loading preview..." text
- **Error state**: When `error`, show the error message in red text with an AlertTriangle icon (from lucide-react)
- **Preview content** (when preview is available):
  - **Header section**: Show title, provider name badge, and truncation warning if `isTruncated` (e.g., "Showing {messages.length} of {totalMessageCount} messages")
  - **Warnings section**: If `preview.warnings.length > 0`, render each warning in an amber/yellow styled box with a `AlertTriangleIcon`. Use `text-amber-600 dark:text-amber-400` for warning text.
  - **Messages section**: Render messages in a compact chat-like format:
    - Each message shows role (bold, "User" or "Assistant"), and text content
    - Truncate long message text to first 3-4 lines with an ellipsis indicator. Use CSS `line-clamp-4` or similar.
    - Alternate background for user vs assistant messages for visual distinction
    - Wrap in a scrollable container (the DialogPanel handles outer scroll, but if there are many messages, add a max-height container)
  - **Activities section**: If activities exist, show a collapsible "Activities ({count})" section listing each activity with its kind badge and summary text
  - **Stats footer**: Show total message count, total activity count, and link mode badge

Style the component using Tailwind classes consistent with the codebase. Use `Badge` from `~/components/ui/badge` for provider and link mode indicators.

**2. Create `ImportOptionsStep.tsx` (Step 4):**

Props: `{ importOptions: ImportOptions; selectedSession: HistoryImportConversationSummary; dispatch: React.Dispatch<WizardAction> }`

The step renders a form with the following fields:

- **Title** (text input): Default to `importOptions.title` (which should be pre-filled from the session summary title when SELECT_SESSION fires). Use a standard `<input>` with Tailwind classes: `w-full rounded-md border border-input bg-background px-3 py-2 text-sm`. On change, dispatch `UPDATE_OPTIONS` with `{ title: newValue }`.

- **Model** (text input or select): Default to `importOptions.model`. Use a text input for simplicity (model slugs vary by provider). On change, dispatch `UPDATE_OPTIONS` with `{ model: newValue }`.

- **Runtime Mode** (select): Options are "full-access" and "approval-required" (from RuntimeMode type). Use the `Select` component from `~/components/ui/select` if available, or a native `<select>` styled with Tailwind. Default: "full-access". On change, dispatch `UPDATE_OPTIONS` with `{ runtimeMode: newValue }`.

- **Interaction Mode** (select): Options are "default" and "plan" (from ProviderInteractionMode type). Default: "default". On change, dispatch `UPDATE_OPTIONS`.

- **Link Mode** (select): Options are "native-resume", "transcript-replay", "snapshot-only" (from HistoryImportLinkMode type). Default from session's linkMode. Show a brief description below each option:
  - native-resume: "Resume the original provider session"
  - transcript-replay: "Continue from imported transcript"
  - snapshot-only: "Read-only snapshot, no continuation"
  On change, dispatch `UPDATE_OPTIONS` with `{ linkMode: newValue }`.

Each field should have a label and be spaced with `space-y-4` or `gap-4`. Add brief helper text under each field explaining the option.
  </action>
  <verify>
1. Run `bun typecheck` -- both files must compile cleanly
2. Run `bun lint` -- no lint errors
3. Verify PreviewStep renders correctly with null/loading/data states
4. Verify ImportOptionsStep renders all 5 form fields with proper types
  </verify>
  <done>
PreviewStep shows message samples, activity samples, truncation warnings, and server warnings with proper loading/error states. ImportOptionsStep shows title, model, runtime mode, interaction mode, and link mode fields with descriptive labels and dispatches UPDATE_OPTIONS on change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ResultStep, wire all steps in ImportWizard, and add toast notification</name>
  <files>
    apps/web/src/components/ImportWizard/steps/ResultStep.tsx
    apps/web/src/components/ImportWizard/ImportWizard.tsx
  </files>
  <action>
**1. Create `ResultStep.tsx` (Step 5):**

Props: `{ result: HistoryImportExecuteResult | null; error: string | null; onNavigateToThread: (threadId: string) => void; onClose: () => void }`

Render:
- **Error state**: If `error`, show an error message with red styling and an AlertCircle icon. Include a "Try Again" or "Close" button.
- **Success state** (when `result` is available):
  - A success icon (CheckCircle2 from lucide-react) in green
  - "Import Complete" heading
  - Stats display:
    - "{messageCount} messages imported" with a MessageSquare icon
    - "{activityCount} activities imported" with an Activity icon
    - "Link mode: {linkMode}" with appropriate badge
    - "Imported at: {importedAt}" formatted as locale string
  - A prominent "Go to Thread" button that calls `onNavigateToThread(result.threadId)` -- use the primary Button variant
  - A secondary "Close" button that calls `onClose`

Style with centered content, appropriate spacing, and celebratory but not excessive design.

**2. Update `ImportWizard.tsx` to wire steps 3-5:**

Replace the placeholder divs for preview/options/result with the real components.

Add these integrations:

**Preview query**: Use `useQuery(historyImportPreviewQueryOptions(state.selectedSession?.catalogId ?? null))` from `~/lib/historyImportReactQuery`. When the query data arrives, dispatch `SET_PREVIEW` with the preview data (use a `useEffect` watching `previewQuery.data`).

**Execute mutation**: Use `useMutation(historyImportExecuteMutationOptions({ queryClient }))` from `~/lib/historyImportReactQuery`. Import `useQueryClient` from `@tanstack/react-query`.

**Step routing update**: Wire the step components:
- "preview": `<PreviewStep preview={state.preview} isLoading={previewQuery.isLoading} error={previewQuery.error?.message ?? state.error} />`
- "options": `<ImportOptionsStep importOptions={state.importOptions} selectedSession={state.selectedSession!} dispatch={dispatch} />`
- "result": `<ResultStep result={state.result} error={state.error} onNavigateToThread={handleNavigateToThread} onClose={onClose} />`

**Footer buttons update**:
- On "preview" step: Next button label "Continue", enabled when preview data is loaded
- On "options" step: Primary button label "Import", onClick triggers the execute mutation:
  ```typescript
  const handleImport = () => {
    if (!state.selectedSession || !projectId) return;
    executeMutation.mutate({
      catalogId: state.selectedSession.catalogId,
      projectId,
      title: state.importOptions.title,
      model: state.importOptions.model,
      runtimeMode: state.importOptions.runtimeMode,
      interactionMode: state.importOptions.interactionMode,
      linkMode: state.importOptions.linkMode,
    });
  };
  ```
  Disable the button while `executeMutation.isPending`. Show a spinner in the button text while pending ("Importing...").
- On "result" step: Only show "Close" and "Go to Thread" buttons (no Back)

**Mutation callbacks**:
- `onSuccess` of the mutation: dispatch `SET_RESULT` with the result, then show toast:
  ```typescript
  // Note: `description` is a built-in @base-ui/react/toast field (rendered by Toast.Description),
  // not from ThreadToastData. Existing usage in useAppUpdate.ts confirms this pattern.
  toastManager.add({
    type: "success",
    title: "Conversation imported",
    description: `Imported ${result.messageCount} messages and ${result.activityCount} activities.`,
  });
  ```
  Import `toastManager` from `~/components/ui/toast`.
- `onError` of the mutation: dispatch `SET_ERROR` with the error message

**Navigation handler**:
```typescript
const navigate = useNavigate();
const handleNavigateToThread = useCallback((threadId: string) => {
  onClose();
  void navigate({ to: "/$threadId", params: { threadId } });
}, [navigate, onClose]);
```

Import `useNavigate` from `@tanstack/react-router`.

**Pre-fill import options from session**: When SELECT_SESSION fires in the reducer, pre-fill importOptions.title with `session.title`, importOptions.model with `session.model ?? getDefaultModel()`, and importOptions.linkMode with `session.linkMode`. Update the reducer accordingly -- in the SELECT_SESSION case, merge these into importOptions.
  </action>
  <verify>
1. Run `bun typecheck` -- all files must compile cleanly
2. Run `bun lint` -- no lint errors
3. Verify the full wizard flow compiles:
   - Step 1 provider select -> Step 2 session list -> Step 3 preview -> Step 4 options -> Step 5 result
4. Verify toast import compiles (toastManager from ui/toast)
5. Verify navigate import compiles (useNavigate from @tanstack/react-router)
  </verify>
  <done>
ResultStep shows import success/error with message counts, link mode, and "Go to Thread" navigation. ImportWizard wires preview query (lazy on selectedSession), execute mutation with pending/error states, toast notification on success, and navigation to created thread. Import options pre-filled from session summary. All 5 wizard steps are fully functional end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `bun typecheck` passes with zero errors
2. `bun lint` passes with zero errors
3. PreviewStep renders message samples with role labels and truncated text
4. PreviewStep shows warnings from server in amber styling
5. ImportOptionsStep renders all 5 configuration fields with appropriate controls
6. ResultStep shows success state with import counts and "Go to Thread" button
7. Execute mutation triggers on "Import" button click with correct parameters
8. Toast notification fires after successful import with message and activity counts
9. Navigation to created thread works from the "Go to Thread" button
10. Error states render gracefully in both preview and execute flows
</verification>

<success_criteria>
- PreviewStep shows transcript messages, activities, truncation notice, and warnings
- ImportOptionsStep renders title, model, runtime mode, interaction mode, and link mode fields
- ResultStep shows import counts, link mode, and navigation to created thread
- Execute mutation wired with loading state, error handling, and success toast
- Import options pre-filled from session summary data
- Full 5-step wizard flow works end-to-end: discover -> browse -> preview -> configure -> import -> navigate
- All code compiles and lints cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-import-ui/03-02-SUMMARY.md`
</output>
