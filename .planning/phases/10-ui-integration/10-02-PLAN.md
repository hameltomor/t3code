---
phase: 10-ui-integration
plan: 02
type: tdd
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/src/components/contextStatusIndicator.logic.ts
  - apps/web/src/components/contextStatusIndicator.logic.test.ts
  - apps/web/src/components/ContextStatusIndicator.tsx
  - apps/web/src/components/ChatView.tsx
autonomous: true

must_haves:
  truths:
    - "Full pill mode shows source label, exact token counts, freshness, and compaction history when VITE_CONTEXT_STATUS_FULL_PILL=true"
    - "Minimal badge is the default when VITE_CONTEXT_STATUS_FULL_PILL is not set or is 'false'"
    - "'Compacted recently' state displayed when last compaction is within 5 minutes"
    - "Stale freshness shows 'Updated Xm ago' relative time"
    - "500ms debounce prevents visual flicker from rapid context status updates"
    - "Unit tests cover all threshold transitions (neutral -> watch -> warning -> danger), unknown state, stale freshness, and compacted state"
  artifacts:
    - path: "apps/web/src/components/contextStatusIndicator.logic.test.ts"
      provides: "Comprehensive unit tests for all context status derivation logic"
      contains: "deriveContextThreshold"
    - path: "apps/web/src/components/contextStatusIndicator.logic.ts"
      provides: "Extended logic with compaction recency, stale freshness, full pill data"
      exports: ["deriveContextThreshold", "deriveContextStatusDisplay"]
    - path: "apps/web/src/components/ContextStatusIndicator.tsx"
      provides: "Full pill mode with tooltip, hysteresis debounce"
      exports: ["ContextStatusIndicator"]
  key_links:
    - from: "apps/web/src/components/contextStatusIndicator.logic.test.ts"
      to: "apps/web/src/components/contextStatusIndicator.logic.ts"
      via: "Tests import and verify all exported derivation functions"
      pattern: "import.*contextStatusIndicator\\.logic"
    - from: "apps/web/src/components/ContextStatusIndicator.tsx"
      to: "@tanstack/react-pacer"
      via: "useDebouncedValue for 500ms hysteresis"
      pattern: "useDebouncedValue"
---

<objective>
Add full pill mode (feature-flagged), edge state handling, hysteresis, and comprehensive rendering tests.

Purpose: Complete the context status UI with all edge cases (stale, compacted, unknown), the internal-only full pill mode, visual stability via debounce, and tests proving correctness across all threshold transitions and states.

Output: Full pill mode behind feature flag, all edge states handled, 500ms debounce applied, comprehensive test suite.
</objective>

<execution_context>
@/home/danil.morozov/.claude/get-shit-done/workflows/execute-plan.md
@/home/danil.morozov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-integration/10-RESEARCH.md
@.planning/phases/10-ui-integration/10-01-SUMMARY.md

@apps/web/src/components/contextStatusIndicator.logic.ts
@apps/web/src/components/ContextStatusIndicator.tsx
@apps/web/src/components/ChatView.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/tooltip.tsx
@apps/web/src/session-logic.ts
@apps/web/src/session-logic.test.ts
@packages/contracts/src/orchestration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend logic module with edge states and full pill data, add comprehensive tests</name>
  <files>
    apps/web/src/components/contextStatusIndicator.logic.ts
    apps/web/src/components/contextStatusIndicator.logic.test.ts
  </files>
  <action>
**RED phase -- write tests first, they MUST fail:**

Create `apps/web/src/components/contextStatusIndicator.logic.test.ts`:

Import `deriveContextThreshold`, `deriveContextStatusDisplay` from `./contextStatusIndicator.logic`.

Write test suites:

1. `describe("deriveContextThreshold")`:
   - `it("returns neutral for undefined percent")` -- `expect(deriveContextThreshold(undefined)).toBe("neutral")`
   - `it("returns neutral for 0%")` -- `expect(deriveContextThreshold(0)).toBe("neutral")`
   - `it("returns neutral for 69%")` -- `expect(deriveContextThreshold(69)).toBe("neutral")`
   - `it("returns watch at 70%")` -- `expect(deriveContextThreshold(70)).toBe("watch")`
   - `it("returns watch at 84%")` -- `expect(deriveContextThreshold(84)).toBe("watch")`
   - `it("returns warning at 85%")` -- `expect(deriveContextThreshold(85)).toBe("warning")`
   - `it("returns warning at 94%")` -- `expect(deriveContextThreshold(94)).toBe("warning")`
   - `it("returns danger at 95%")` -- `expect(deriveContextThreshold(95)).toBe("danger")`
   - `it("returns danger at 100%")` -- `expect(deriveContextThreshold(100)).toBe("danger")`

2. `describe("deriveContextStatusDisplay")`:
   - `it("returns invisible when sessionActive is false")` -- pass valid contextStatus but `sessionActive=false`, expect `visible: false`
   - `it("returns invisible when contextStatus is null")` -- pass `null` contextStatus, `sessionActive=true`, expect `visible: false`
   - `it("returns 'Context unknown' when percent is undefined")` -- pass contextStatus without percent, sessionActive=true, expect `label: "Context unknown"`, `visible: true`
   - `it("returns 'Context XX%' with rounded percent")` -- pass percent=42.7, expect `label: "Context 43%"`
   - `it("derives correct threshold from percent")` -- pass percent=86, expect `threshold: "warning"`
   - `it("detects compacted recently when lastCompactedAt is within 5 minutes")` -- pass `lastCompactedAt` set to `nowMs - 2 * 60 * 1000` (2 min ago), expect `compactedRecently: true`
   - `it("does not flag compacted when lastCompactedAt is older than 5 minutes")` -- pass `lastCompactedAt` set to `nowMs - 10 * 60 * 1000` (10 min ago), expect `compactedRecently: false`
   - `it("detects stale freshness")` -- pass `freshness: "stale"`, expect `isStale: true`
   - `it("does not flag stale for live freshness")` -- pass `freshness: "live"`, expect `isStale: false`
   - `it("computes lastUpdatedLabel as relative time when stale")` -- pass `measuredAt` set to 3 min ago, `freshness: "stale"`, expect `lastUpdatedLabel` to be a non-null string like "3m ago" or similar

Create a factory function `makeContextStatus(overrides)` that produces a valid `OrchestrationThreadContextStatus`-shaped object with defaults:
```typescript
function makeContextStatus(overrides: Partial<OrchestrationThreadContextStatus> = {}) {
  return {
    provider: "codex" as const,
    support: "native" as const,
    source: "provider-event" as const,
    freshness: "live" as const,
    status: "ok" as const,
    model: "gpt-5.3-codex",
    tokenUsage: { totalTokens: 5000 },
    percent: 42,
    measuredAt: new Date().toISOString(),
    ...overrides,
  };
}
```

Run tests -- they should fail on the new fields (`compactedRecently`, `isStale`, `lastUpdatedLabel`) since the logic module from 10-01 does not yet include them.

**GREEN phase -- extend the logic module:**

Update `apps/web/src/components/contextStatusIndicator.logic.ts`:

1. Add `nowMs: number` parameter to `deriveContextStatusDisplay` (third parameter).
2. Extend `ContextStatusDisplay` interface with:
   - `compactedRecently: boolean` -- true if lastCompactedAt is within 5 minutes of nowMs
   - `isStale: boolean` -- true if freshness is "stale"
   - `lastUpdatedLabel: string | null` -- relative time string like "2m ago" when stale, null otherwise
   - `compactionCount: number` -- from contextStatus, default 0
   - `lastCompactedAt: string | null` -- ISO string or null
   - `sourceLabel: string` -- human-readable source (e.g., "Codex (native)" or "Claude Code (derived)")
   - `tokenDetail: string | null` -- e.g., "5,000 / 128,000 tokens" or null if no data
3. Add named constant `COMPACTION_RECENCY_THRESHOLD_MS = 5 * 60 * 1000`.
4. In `deriveContextStatusDisplay`:
   - Compute `compactedRecently` from `lastCompactedAt` and `nowMs`.
   - Compute `isStale` from `freshness === "stale"`.
   - Compute `lastUpdatedLabel`: when stale, format relative time from `measuredAt` vs `nowMs` (e.g., "2m ago", "1h ago"). Use simple math: diff in ms -> minutes -> "Xm ago" if < 60m, else "Xh ago". When not stale, null.
   - Compute `sourceLabel`: map provider + support to human-readable label (e.g., "codex" + "native" -> "Codex (native)", "claudeCode" + "derived-live" -> "Claude Code (derived)").
   - Compute `tokenDetail`: if tokenUsage?.totalTokens is defined and contextWindowLimit is defined, format as "X,XXX / Y,YYY tokens". If only totalTokens, format as "X,XXX tokens". If neither, null.
   - Compute `compactionCount` from contextStatus.compactionCount ?? 0.
   - Compute `lastCompactedAt` from contextStatus.lastCompactedAt ?? null.

Run tests -- all tests should now pass.

**REFACTOR phase:** Clean up any formatting, ensure consistent code style.
  </action>
  <verify>
Run `bun run test -- apps/web/src/components/contextStatusIndicator.logic.test.ts` -- all tests pass.
Run `bun typecheck` -- must pass.
  </verify>
  <done>
All context status derivation edge cases are tested: threshold transitions at every boundary (0, 69, 70, 84, 85, 94, 95, 100), unknown state (undefined percent), compaction recency (within and beyond 5 minutes), stale freshness detection and relative time formatting, and invisible states (no session, null context status). Logic module exports extended ContextStatusDisplay with all fields needed for full pill mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add full pill mode, hysteresis debounce, and Tooltip rendering</name>
  <files>
    apps/web/src/components/ContextStatusIndicator.tsx
    apps/web/src/components/ChatView.tsx
  </files>
  <action>
1. Update `apps/web/src/components/ContextStatusIndicator.tsx`:
   - Add feature flag: `const FULL_PILL_MODE = import.meta.env.VITE_CONTEXT_STATUS_FULL_PILL === "true";`
   - Import `Tooltip` primitives from `./ui/tooltip` (follow existing tooltip usage in the codebase).
   - Import `Separator` from `./ui/separator`.
   - The component receives `display: ContextStatusDisplay` as props.
   - **Minimal badge mode** (default, when `!FULL_PILL_MODE`):
     - Render `<Badge variant={THRESHOLD_BADGE_VARIANT[display.threshold]} size="sm">{display.label}</Badge>`
     - If `display.compactedRecently`, append a small indicator (e.g., a sparkle/refresh icon or text " (compacted)") after the label text.
     - If `display.isStale && display.lastUpdatedLabel`, show the lastUpdatedLabel as a secondary text: e.g., `<Badge>Context 42% · 2m ago</Badge>`.
   - **Full pill mode** (when `FULL_PILL_MODE`):
     - Wrap the badge in a Tooltip.
     - Tooltip content shows:
       - Source label (e.g., "Codex (native)")
       - Token detail (e.g., "5,000 / 128,000 tokens") if available
       - Freshness (e.g., "Live" or "Stale · Updated 2m ago")
       - Compaction info: "Compacted X times" + "Last compacted: [timestamp]" if applicable
     - Badge itself shows same label as minimal mode but can include source label prefix: e.g., "Codex · Context 42%"

2. Update `apps/web/src/components/ChatView.tsx`:
   - Apply 500ms debounce (UI-07 hysteresis) to the context status display:
     - `useDebouncedValue` is already imported from `@tanstack/react-pacer`.
     - After computing `contextStatusDisplay` with `useMemo`, wrap it: `const [debouncedContextStatusDisplay] = useDebouncedValue(contextStatusDisplay, { wait: 500 });`
     - Pass `debouncedContextStatusDisplay` (not the raw display) to `ContextStatusIndicator`.
   - Update the `deriveContextStatusDisplay` call to pass `Date.now()` as the third argument (nowMs).
   - Ensure the nowMs is NOT causing unnecessary re-renders -- it should only recompute when `thread.contextStatus` or `sessionActive` change, not on every render. Use the `useMemo` deps array `[thread.contextStatus, sessionActive]` (Date.now() computed inside the memo callback body, not as a dep).
  </action>
  <verify>
Run `bun typecheck` -- must pass.
Run `bun lint` -- must pass.
Run `bun run test -- apps/web/src/components/contextStatusIndicator.logic.test.ts` -- all tests pass.
  </verify>
  <done>
Full pill mode is feature-flagged behind `VITE_CONTEXT_STATUS_FULL_PILL` env var (defaults to off). Tooltip in full pill mode shows source label, token counts, freshness, and compaction history. 500ms debounce applied via `useDebouncedValue` prevents visual flicker from rapid updates. Stale freshness shows relative time. Compacted recently indicator visible in both modes. All requirements UI-01 through UI-08 and TEST-03 are satisfied.
  </done>
</task>

</tasks>

<verification>
- `bun typecheck` passes
- `bun lint` passes
- `bun run test -- apps/web/src/components/contextStatusIndicator.logic.test.ts` passes with all tests green
- Full pill mode only active when `VITE_CONTEXT_STATUS_FULL_PILL=true`
- Debounce applied at 500ms on the display value, not the store
- Compaction recency threshold is 5 minutes (named constant)
- Stale freshness shows relative time ("Xm ago")
- All threshold boundary values tested (69->neutral, 70->watch, 84->watch, 85->warning, 94->warning, 95->danger)
</verification>

<success_criteria>
- TEST-03: All rendering tests pass covering unknown state, stale freshness, compacted state, and threshold transitions
- UI-02: Full pill + tooltip mode shows source label, exact token counts, freshness, compaction history
- UI-03: Minimal badge is default; full pill is feature-flagged only
- UI-05: Stale freshness shows last updated time
- UI-06: Compacted recently state displayed within 5-minute threshold
- UI-07: 500ms debounce prevents visual flicker
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-integration/10-02-SUMMARY.md`
</output>
